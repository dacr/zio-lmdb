syntax = "proto3";

package zio.lmdb.protobuf;

// Mirror of data class named UserProfile with JSON serdes - CHANGE IN SYNC
// Used to compare performance between JSON and PROTOBUF
message UserProfilePB {
  string id = 1;
  string name = 2;
  string email = 3;
  int32 age = 4;
}
